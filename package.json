{
  "scripts": {
    "start": "jest --watchAll",
    "lint": "prettier -l \"**/*.{js,json,md}\"",
    "format": "prettier --write \"**/*.{js,json,md}\""
  },
  "prettier": {
    "singleQuote": true,
    "trailingComma": "all"
  },
  "devDependencies": {
    "jest": "^23.6.0"
  },
  "dependencies": {
    "ansi-escapes": "^3.1.0",
    "jest-watcher": "^23.4.0",
    "yargs": "^12.0.5"
  }
}
